<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/form.css">
    <link rel="stylesheet" href= "/css/nav.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <title>Dashborad</title>
</head>
<body>
    <%- include('../partials/nav')  %>
    <h1 id="form-title">Update User</h1>
    <div class="content-wrap">
        <% if (message) { %>
            <div id="message">
                <p><%= message %></p>
                <i class="bi bi-x"></i>
            </div>
        <% } %>
        <div id="form-wrap">
            <form action="/users/update/<%= id %>" method="post">
                <div class="input-wrap">
                    <i class="bi bi-person"></i>
                    <input type="text" value="<%= data.username %>" name="username" placeholder="Username" maxlength="25">
                </div>
                <div class="input-wrap">
                    <i class="bi bi-envelope"></i>
                    <input type="email" value="<%= data.email %>" name="email" placeholder="Email" maxlength="50">
                </div>
                <div class="input-wrap">
                    <i class="bi bi-key"></i>
                    <input type="password" value="<%= data.password %>" name="password" placeholder="Password" maxlength="25">
                </div>
                <button type="submit">Update</button>
            </form>
        </div>
    </div>

    <script src="js/form.js"></script>
</body>
</html>